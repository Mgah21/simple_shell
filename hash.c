#include "shell.h"
/**
 * hash - #
 * @buff: input;
 * Return:void
 */
void hash(void)
{
int h;
 char *buffer;
for (h = 0; buffer[h] != '\0'; h++)
{
if (buffer [h] == '#')
{
buffer[i] = '\0';
break;
}
}
}
